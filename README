This project needs to add the followoing property sheets:

- openNI.prop
- openCV.prop
- boost.prop


It also needs to add the project KinectUtils.


Inside KinectUtils there is a class called filePaths. These paths will need to be updated  with your own paths.
